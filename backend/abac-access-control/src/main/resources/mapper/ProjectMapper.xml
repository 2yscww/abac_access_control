<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xie.platform.mapper.ProjectMapper">

    <!-- 插入项目 -->
    <insert id="insert" parameterType="com.xie.platform.model.Projects">
        INSERT INTO projects (
            project_id,
            project_name,
            project_phase,
            security_level,
            created_by_employee_id
        ) VALUES (
            #{projectId},
            #{projectName},
            #{projectPhase},
            #{securityLevel},
            #{createdByEmployeeId}
        )
    </insert>

    <!-- 根据项目ID查询 -->
    <select id="selectById" resultType="com.xie.platform.model.Projects">
        SELECT
            project_id,
            project_name,
            project_phase,
            security_level,
            created_by_employee_id,
            created_at,
            updated_at
        FROM projects
        WHERE project_id = #{projectId}
    </select>

    <!-- 根据项目名称查询 -->
    <select id="selectByName" resultType="com.xie.platform.model.Projects">
        SELECT
            project_id,
            project_name,
            project_phase,
            security_level,
            created_by_employee_id,
            created_at,
            updated_at
        FROM projects
        WHERE project_name = #{projectName}
    </select>

    <!-- 条件查询项目列表 -->
    <select id="selectByCondition" resultType="com.xie.platform.model.Projects">
        SELECT
            project_id,
            project_name,
            project_phase,
            security_level,
            created_by_employee_id,
            created_at,
            updated_at
        FROM projects
        <where>
            <if test="projectName != null and projectName != ''">
                AND project_name LIKE CONCAT('%', #{projectName}, '%')
            </if>
            <if test="projectPhase != null">
                AND project_phase = #{projectPhase}
            </if>
            <if test="securityLevel != null">
                AND security_level = #{securityLevel}
            </if>
            <if test="createdByEmployeeId != null">
                AND created_by_employee_id = #{createdByEmployeeId}
            </if>
        </where>
        ORDER BY created_at DESC
        <if test="pageSize != null and pageNum != null">
            LIMIT #{pageSize} OFFSET #{pageNum}
        </if>
    </select>

    <!-- 统计符合条件的项目数量 -->
    <select id="countByCondition" resultType="int">
        SELECT COUNT(*)
        FROM projects
        <where>
            <if test="projectName != null and projectName != ''">
                AND project_name LIKE CONCAT('%', #{projectName}, '%')
            </if>
            <if test="projectPhase != null">
                AND project_phase = #{projectPhase}
            </if>
            <if test="securityLevel != null">
                AND security_level = #{securityLevel}
            </if>
            <if test="createdByEmployeeId != null">
                AND created_by_employee_id = #{createdByEmployeeId}
            </if>
        </where>
    </select>

    <!-- 更新项目阶段 -->
    <update id="updatePhase">
        UPDATE projects
        SET project_phase = #{newPhase}
        WHERE project_id = #{projectId}
    </update>

    <!-- 删除项目 -->
    <delete id="deleteById">
        DELETE FROM projects
        WHERE project_id = #{projectId}
    </delete>

</mapper>
