<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xie.platform.mapper.ProjectMapper">

    <insert id="insert" parameterType="com.xie.platform.model.Projects"
            useGeneratedKeys="true" keyProperty="projectId">
        INSERT INTO projects (
            project_name,
            project_phase,
            security_level,
            created_by_employee_id,
            owner_id
        ) VALUES (
            #{projectName},
            #{projectPhase},
            #{securityLevel},
            #{createdByEmployeeId},
            #{ownerId}
        )
    </insert>

    <select id="selectById" resultType="com.xie.platform.model.Projects">
        SELECT
            project_id,
            project_name,
            project_phase,
            security_level,
            created_by_employee_id,
            owner_id,
            created_at,
            updated_at
        FROM projects
        WHERE project_id = #{projectId}
    </select>

    <select id="selectByName" resultType="com.xie.platform.model.Projects">
        SELECT
            project_id,
            project_name,
            project_phase,
            security_level,
            created_by_employee_id,
            owner_id,
            created_at,
            updated_at
        FROM projects
        WHERE project_name = #{projectName}
    </select>

    <select id="selectByCondition" resultType="com.xie.platform.model.Projects">
        SELECT
            project_id,
            project_name,
            project_phase,
            security_level,
            created_by_employee_id,
            owner_id,
            created_at,
            updated_at
        FROM projects
        <where>
            <if test="projectName != null and projectName != ''">
                AND project_name LIKE CONCAT('%', #{projectName}, '%')
            </if>
            <if test="projectPhase != null">
                AND project_phase = #{projectPhase}
            </if>
            <if test="securityLevel != null">
                AND security_level = #{securityLevel}
            </if>
            <if test="createdByEmployeeId != null">
                AND created_by_employee_id = #{createdByEmployeeId}
            </if>
        </where>
        ORDER BY created_at DESC
        <if test="pageSize != null and pageNum != null">
            LIMIT #{pageSize} OFFSET #{pageNum}
        </if>
    </select>

    <select id="countByCondition" resultType="int">
        SELECT COUNT(*)
        FROM projects
        <where>
            <if test="projectName != null and projectName != ''">
                AND project_name LIKE CONCAT('%', #{projectName}, '%')
            </if>
            <if test="projectPhase != null">
                AND project_phase = #{projectPhase}
            </if>
            <if test="securityLevel != null">
                AND security_level = #{securityLevel}
            </if>
            <if test="createdByEmployeeId != null">
                AND created_by_employee_id = #{createdByEmployeeId}
            </if>
        </where>
    </select>

    <select id="selectByPhaseCodes" resultType="com.xie.platform.model.Projects">
        SELECT
            project_id,
            project_name,
            project_phase,
            security_level,
            created_by_employee_id,
            owner_id,
            created_at,
            updated_at
        FROM projects
        WHERE project_phase IN
        <foreach collection="phaseCodes" item="phaseCode" open="(" separator="," close=")">
            #{phaseCode}
        </foreach>
        ORDER BY project_id
    </select>

    <update id="updatePhase">
        UPDATE projects
        SET project_phase = #{newPhase},
            owner_id = #{ownerId}
        WHERE project_id = #{projectId}
    </update>

    <update id="updateOwnerByPhaseCodes">
        UPDATE projects
        SET owner_id = #{ownerId}
        WHERE project_phase IN
        <foreach collection="phaseCodes" item="phaseCode" open="(" separator="," close=")">
            #{phaseCode}
        </foreach>
    </update>

    <delete id="deleteById">
        DELETE FROM projects
        WHERE project_id = #{projectId}
    </delete>

</mapper>
